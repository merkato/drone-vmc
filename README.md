# drone-vmc
Stack do obsługi streamingu danych z drona.

## Instalacja

* Uruchom ```setup.sh```
* Wykonaj ```newgrp docker``` a następnie dla pewności przeładuj terminal
* Wykonaj ```docker compose up -d --build```

## Uruchomienie
Pod adresem dasboard.twojadomena znajdziesz usługę
